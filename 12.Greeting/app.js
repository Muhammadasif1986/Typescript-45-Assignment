"use strict";
// 12.	Greetings: Start with the array you used in Exercise 11, 
// but instead of just printing each person’s name,
//  print a message to them. The text of each message should be the same,
//  but each message should be personalized with the person’s name.
let Friend_list = ['Akber', 'Sajid', 'Ayaz', 'Abid', 'Asif'];
const messageFriend = ("Ramdan Mubarak  ");
for (let i = 0; i < Friend_list.length; i++) {
    console.log(messageFriend + Friend_list[i]);
}
